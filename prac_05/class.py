# from operator import itemgetter
#
# # Correct the dictionary definition to use proper syntax
# data = {'Derek': 7, 'Xavier': 80, 'Bob': 612, 'Chantanelle': 97}
#
# # Calculate the maximum length of keys and values
# name_space = max(len(key) for key in data.keys())
# age_space = max(len(str(value)) for value in data.values())
#
# # Sort the items in descending order based on the values and print them
# for key, value in sorted(data.items(), key=itemgetter(1), reverse=True):
#     print(f"{key:<{name_space}} = {value:<{age_space}}")  # Left-align output

name = {"Tom", "Jun", "Anthony"}
